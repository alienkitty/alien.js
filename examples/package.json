{
  "name": "alien-build-projects",
  "version": "1.0.5",
  "author": "Patrick Schroen",
  "private": true,
  "scripts": {
    "build": "find . -name 'rollup.config.js' -maxdepth 2 ! -path './alien.js*' -execdir rollup -c {} \\;",
    "pngquant": "for x in `find . -name 'rollup.config.js' -maxdepth 2 ! -path './alien.js*' -exec dirname {} \\;`; do echo \"$x\"; cd \"$x\"; rm -f dist/assets/*.png; cp src/images/*.png dist/assets/; find dist/assets -name '*.png' -exec pngquant --strip --ext=.png --force {} \\; cd ..; done"
  },
  "devDependencies": {
    "babel-preset-es2015-rollup": "^3.0.0",
    "eslint": "^4.2.0",
    "replace": "^0.3.0",
    "rollup": "^0.45.2",
    "rollup-plugin-babel": "^2.7.1",
    "rollup-plugin-eslint": "^4.0.0",
    "rollup-plugin-uglify": "^2.0.1",
    "rollup-watch": "^4.3.1",
    "uglify-es": "^3.0.25"
  }
}
